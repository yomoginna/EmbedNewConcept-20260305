{
    "exists": true,
    "title": "Greedy triangulation",
    "url": "https://en.wikipedia.org/wiki/Greedy_triangulation",
    "summary": "The greedy triangulation is a method to compute a polygon triangulation or a point set triangulation using a greedy algorithm, which adds edges one by one to the solution in strict increasing order by length, with the condition that an edge cannot cut a previously inserted edge.\n\n\n== References ==",
    "text": "The greedy triangulation is a method to compute a polygon triangulation or a point set triangulation using a greedy algorithm, which adds edges one by one to the solution in strict increasing order by length, with the condition that an edge cannot cut a previously inserted edge."
}